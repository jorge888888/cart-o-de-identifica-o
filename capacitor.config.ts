import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cartao-identificacao-calouros',
  webDir: 'www'
};

export default config;
